/**
 * Exports the setups:
 * - the application setup
 * - the setup of the clusters (if necessary)
 */
module.exports.setup      = require('./setup');
module.exports.clusterize = require('./clusterize');